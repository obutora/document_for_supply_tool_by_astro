---
import OpenButton from "../components/menu/OpenButton";

export interface Props {
  title: string;
}

const { title } = Astro.props;
const posts = await Astro.glob("../pages/supplyTools/*/*.mdx");
const subPosts = await Astro.glob("../pages/supplyTools/*/*/*.mdx");
// const posts = await Astro.glob("../pages/supplyTools/*.mdx");
// console.log(posts.length);
// console.log(posts[0].frontmatter);
// console.log(posts[1].frontmatter);
// console.log(posts[1].getHeadings());
console.log(subPosts[0].url);
---

<!DOCTYPE html>
<html lang="ja">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="generator" content={Astro.generator} />
    <title>{title}</title>
  </head>
  <body>
    <header>
      <div class="h-8 w-8 rounded-full bg-sky-600 mr-4">
      </div>発注点設定ツールマニュアル
    </header>
    <div class="flex gap-8">
      <div class="sidebar">
        <OpenButton client:load posts={posts} subPosts={subPosts} />
      </div>
      <article>
        <slot />
      </article>
    </div>
    <style is:global>
      :root {
        --accent: 124, 58, 237;
        --accent-gradient: linear-gradient(
          45deg,
          rgb(var(--accent)),
          #da62c4 30%,
          white 60%
        );
      }
      html {
        font-family: "游ゴシック体", YuGothic, "游ゴシック", "Yu Gothic",
          "Helvetica Neue", Arial, "Hiragino Kaku Gothic ProN", "Hiragino Sans",
          Meiryo, sans-serif;
        @apply w-[100vw] h-[100vh] overflow-hidden;
      }
      code {
        font-family: Menlo, Monaco, Lucida Console, Liberation Mono,
          DejaVu Sans Mono, Bitstream Vera Sans Mono, Courier New, monospace;
      }

      body {
        -webkit-text-size-adjust: 100%;
        @apply prose lg:prose-xl antialiased prose-zinc;
      }

      header {
        @apply h-20 flex items-center px-8 sticky w-screen text-zinc-700 bg-white font-bold tracking-wider border-b-2 border-zinc-200;
      }

      a {
        @apply hover:text-sky-500 transition;
      }

      aside {
        @apply mt-4;
      }

      aside a {
        @apply text-xl no-underline text-zinc-600 font-semibold tracking-wide hover:underline underline-offset-2 hover:text-sky-500 transition;
      }

      section a {
        @apply no-underline text-zinc-600 text-lg;
      }

      section li {
        @apply list-none;
      }

      article {
        @apply pt-12 pl-12 shrink-0;
      }

      h1 {
        @apply font-bold;
      }

      .sidebar {
        @apply bg-zinc-100 w-[260px] h-screen shrink-0 pl-8 pt-4;
      }
    </style>
  </body>
</html>
